import SwiftUI

enum DemoTheme {
    static let background = Color(red: 0.08, green: 0.08, blue: 0.08)
    static let sectionTitle = Color.white.opacity(0.9)
    static let sectionIcon = Color.white.opacity(0.8)
    static let imagePlaceholder = Color.white.opacity(0.08)
}
